# JAYU_JAR — dependencias Python (Fase 1 core + terminal)
# Instala:  python -m pip install -r requirements.txt

# Core (imprescindibles)
httpx>=0.27          # cliente HTTP para proveedores LLM y APIs
PyYAML>=6.0          # configuración centralizada en YAML (config/)

# Terminal / UX
colorama>=0.4 ; platform_system == "Windows"   # colores en consola Windows

# Tests (entorno de desarrollo)
pytest>=8.0
pytest-cov>=5.0

# --- Dependencias opcionales por fase (NO necesarias para Fase 1) ---
# Fase 2  (voz):            faster-whisper, piper-tts, webrtcvad
# Fase 3  (web):            playwright
# Fase 4  (pc):             pywinauto, pyautogui
# Fase 5  (mercados):       pandas, numpy (ya presentes en tu máquina)
# Fase 6  (MT5):            MetaTrader5  (ya instalado en tu máquina)
# Fase 7  (visión):         opencv-python, mss
# Memoria vectorial (LTM):  chromadb